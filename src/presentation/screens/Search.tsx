import { Text } from "react-native";
import { Container } from "../components/Container";

export const Search = () => {
    return (
        <Container>
            <Text>Search</Text>
        </Container>
    );
}